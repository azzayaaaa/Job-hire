[phases.setup]
nixPkgs = ["nodejs_22", "pnpm"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@10.28.0 --activate",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["echo Backend workspace ready"]

[start]
cmd = "pnpm backend:start"
